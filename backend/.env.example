# Used when running the backend directly on the host.
DATABASE_URL=postgresql+psycopg://ludex:ludex@localhost:5432/ludex

# Browser origin allowed to call the backend.
FRONTEND_ORIGIN=http://localhost:5173
